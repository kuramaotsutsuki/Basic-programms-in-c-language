#include <stdio.h> //pre processor command/statement

int main() // program start
{
    printf("%lu", sizeof(int));  // %lu is for checking lenght, sizeof is operator which will return size of int in bytes ,, you can chage the int to float and other datatypes to see the size of them
    return 0; // ending program
}
