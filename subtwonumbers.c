#include <stdio.h> // pre processor command

int main()   //program start
{
    int a, b;  // assigning value for input
    printf("enter a nuber a");
    scanf("%d", &a); //scanf is used for taking input 

    printf("enter a nuber b");
    scanf("%d", &b);

    print("The sum of the inputted number is %d\n", a+b);
    
    return 0;  //program end 
}
